<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FluxLEX</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="ios-glass-card">
        <!-- Header with ScanUX Branding -->
        <div class="d-flex justify-content-between align-items-center mb-4">
            <div>
                <h1 class="mb-0">Flux<span style="color: var(--ios-blue);">LEX</span></h1>
                <p class="text-muted" style="font-weight: 500;">Lexical Analysis & Symbol Table</p>
            </div>
                  <div class="badge bg-white text-primary rounded-pill px-3 py-2 shadow-sm border d-flex align-items-center gap-2">
                <span style="height: 8px; width: 8px; background-color: #32d74b; border-radius: 50%; display: inline-block; box-shadow: 0 0 8px #32d74b;"></span>
                Lexical Engine
            </div>
        </div>

        <form action="/analyze" method="POST">
            <div class="mb-4">
                <textarea name="code" class="form-control terminal-box" rows="12" placeholder="// Paste your code here..."></textarea>
            </div>
            <button type="submit" class="btn-liquid">Run Analysis</button>
        </form>
    </div>
</body>
</html>
