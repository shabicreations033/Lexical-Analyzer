<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>FluxLex Results</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="/style.css">
</head>
<body>
    <div class="ios-glass-card">
        <h2 class="mb-4">Analysis <span style="color: var(--ios-blue);">Output</span></h2>
        
        <div class="terminal-box">
            <pre style="margin: 0; color: inherit; font-family: inherit;"><% 
                const lines = output.split('\n');
                lines.forEach(line => {
                    if (line.includes('[LEXICAL ERROR]')) { 
                        %><span class="error-text text-danger"><%= line %></span>
<%                  } else { 
                        %><%= line %>
<%                  }
                }); 
            %></pre>
        </div>
	
	<div class="mt-5 d-flex justify-content-between align-items-center">
    <a href="/" class="btn-liquid" style="width: auto; padding: 12px 40px;">New Analysis</a>
    <p class="text-muted small mb-0">FluxLEX &copy; 2026</p>
</div>
    </div>
</body>
</html>
